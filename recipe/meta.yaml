{% set name = "dandi" %}
{% set version = "0.16.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: 6719a51378571f8af3f41080b67f3d73fe350ee9ec2147b98feb26f2ee42d43e

build:
  number: 0
  skip: true  # [py<37]
  entry_points:
    - dandi=dandi.cli.command:main
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python
    - pip
    - setuptools
  run:
    - python
    - girder-client
    - pynwb >=1.0.3,!=1.1.0
    - appdirs
    - click
    - click-didyoumean
    - email_validator
    - etelemetry
    - fasteners
    - fscacher
    - h5py ~=2.9
    - humanize
    - joblib
    - jsonschema
    - keyring
    - keyrings.alt
    - pycryptodomex
    - pydantic
    - pyout
    - python-dateutil
    - requests >=0.22.0
    - ruamel.yaml
    - semantic_version
    - tenacity
    - tqdm

test:
  requires:
    - docker-compose  # [not win]
    - git
    - pyfakefs
    - pytest
    - pytest-mock
    - responses
  imports:
    - dandi
  commands:
    - python -m pytest -s -v --pyargs dandi

about:
  home: https://github.com/dandi/dandi-cli
  license: Apache-2.0
  license_family: Apache
  license_file: LICENSE
  summary: Command line client for interaction with DANDI archive elements
  dev_url: https://github.com/dandi/dandi-cli

extra:
  recipe-maintainers:
    - yarikoptic
    - jwodder
